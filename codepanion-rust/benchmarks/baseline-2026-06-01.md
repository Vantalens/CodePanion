# Rust Daemon Baseline - 2026-06-01

Command:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\measure-baseline.ps1
```

Environment:

- Workspace: `D:\CodePanion\codepanion-rust`
- Build profile: `release`
- Daemon port: `7791`
- Rust daemon version: `0.1.0`

Results:

| Metric | Value |
| --- | ---: |
| Binary size | 163,328 bytes / 0.16 MiB |
| Cold start to `/health` | 499 ms |
| Working set after start | 5,705,728 bytes / 5.44 MiB |
| `/health` latency | 1 ms |
| WebSocket hello latency | 70 ms |

Acceptance targets:

| Target | Baseline | Status |
| --- | ---: | --- |
| daemon binary < 20 MiB | 0.16 MiB | pass |
| daemon idle memory < 50 MiB | 5.44 MiB | pass |
| daemon cold start < 500 ms | 499 ms | pass |

Notes:

- The script builds `codepanion-daemon` in release mode, launches it locally, probes `/health`, performs a raw WebSocket upgrade smoke check, then stops the daemon.
- The cold-start value is close to the target and should be watched as dependencies are added.
