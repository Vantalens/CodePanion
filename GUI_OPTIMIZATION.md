# GUI 界面优化 - 项目管理整合

## 变更摘要

将项目管理从独立入口整合到设置对话框中，优化界面布局，使其更符合现代 AI 工具的设计理念。

## 主要变更

### 1. 新增顶栏
- **位置**: 整个应用顶部
- **内容**:
  - 左侧：应用名称 "CodePanion"
  - 中间：项目选择器（全局上下文）
  - 右侧：连接状态

### 2. 侧边栏优化
**移除**：
- ❌ 项目选择下拉框（原来在侧边栏）
- ❌ 项目管理按钮
- ❌ 会话计数标签

**保留**：
- ✅ 新建对话按钮（更大，更醒目）
- ✅ 会话列表
- ✅ 底部菜单（工作流、设置）

### 3. 设置对话框重组
**新的标签页顺序**：
1. **项目** - 项目管理（从独立对话框移入）
2. **Providers** - Provider 配置
3. **模型配置** - 模型和角色绑定

### 4. 布局结构
```
┌─────────────────────────────────────────┐
│  CodePanion   [项目: MyProject]   ●已连接  │  ← 顶栏
├──────┬──────────────────────────────────┤
│ 新建  │                                  │
│ 对话  │                                  │
├──────┤                                  │
│      │        主聊天区域                  │
│ 会话 │                                  │
│ 列表 │                                  │
│      │                                  │
├──────┤                                  │
│ 工作流│                                  │
│ 设置 │                                  │
└──────┴──────────────────────────────────┘
```

## 设计理念

### 为什么要这样改？

1. **项目是全局上下文**
   - 项目不是每个会话独立的配置
   - 它影响整个应用的工作环境
   - 应该在顶栏显示，而不是藏在侧边栏

2. **侧边栏专注会话管理**
   - 类似 ChatGPT / Claude.ai 的设计
   - 侧边栏 = 会话历史 + 导航
   - 不应该混入系统配置

3. **设置集中管理**
   - 项目、Provider、模型都是系统配置
   - 统一在设置对话框管理
   - 降低界面复杂度

## 对比

### 之前（旧设计）
```
侧边栏：
├─ 标题 + 3个按钮（新建、设置、工作流）
├─ 项目选择器 + 管理按钮  ← 位置不合理
├─ 会话列表
└─ 连接状态

设置对话框：
├─ Providers
└─ 模型配置
```

### 现在（新设计）
```
顶栏：
├─ 应用名称
├─ 项目选择器（全局）      ← 更合理的位置
└─ 连接状态

侧边栏：
├─ 新建对话（大按钮）
├─ 会话列表
└─ 工作流、设置（底部菜单）

设置对话框：
├─ 项目                    ← 统一管理
├─ Providers
└─ 模型配置
```

## 用户体验改进

### 项目切换
- **之前**: 需要点击侧边栏的项目下拉框
- **现在**: 顶栏随时可见，一键切换

### 项目管理
- **之前**: 侧边栏的管理按钮 → 独立对话框
- **现在**: 设置按钮 → 设置对话框 → 项目标签页

### 视觉清晰度
- **之前**: 侧边栏内容拥挤，项目选择器不显眼
- **现在**: 顶栏项目选择器显眼，侧边栏简洁清爽

## CSS 变更

### 新增样式
- `#topbar` - 顶栏容器
- `.topbar-left/center/right` - 顶栏三区布局
- `.app-title-small` - 顶栏应用名称
- `.project-selector` - 顶栏项目选择器
- `.new-chat-btn` - 更大的新建对话按钮
- `.sidebar-menu-btn` - 侧边栏菜单按钮

### 移除样式
- `.sidebar-header` 中的多按钮布局
- `.sidebar-project` - 侧边栏项目区域
- `.sessions-header` - 会话列表头部

## JavaScript 变更

### DOM 元素
```javascript
// 移除
- projectManageBtn
- sessionCount

// 保留
+ projectSelect (现在在顶栏)
+ settingsBtn (现在在侧边栏底部)
```

### 事件监听器
```javascript
// 移除
- projectManageBtn.click → 打开项目对话框

// 新增
settingsBtn.click → 打开设置对话框并加载项目列表
```

## 兼容性

### 完全向后兼容
- ✅ 项目 API 调用不变
- ✅ Provider API 调用不变
- ✅ 模型配置 API 调用不变
- ✅ 所有现有功能保留

### 复用现有代码
- ✅ `projects.js` - 项目管理逻辑
- ✅ `settings.js` - 设置管理逻辑
- ✅ 项目表单对话框（project-form-dialog）

## 参考设计

这次改进参考了以下现代 AI 工具的设计：

1. **ChatGPT** - 顶栏显示全局上下文，侧边栏纯会话列表
2. **Claude.ai** - 顶栏项目选择器，设置集中管理
3. **Cursor** - 顶栏工作区选择，侧边栏对话历史
4. **VS Code** - 顶栏全局状态，侧边栏功能导航

## 总结

这次优化让 CodePanion 的界面更加**清晰**、**专注**、**符合用户习惯**：

- 📍 **顶栏** = 全局上下文（项目、连接状态）
- 📋 **侧边栏** = 会话管理 + 功能导航
- ⚙️ **设置** = 系统配置集中管理

用户不再需要在多个地方寻找项目管理功能，所有配置都在设置中，所有对话都在侧边栏。
