<Window x:Class="CodePanion.Gui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="CodePanion" Height="700" Width="1200"
        WindowStartupLocation="CenterScreen"
        Closing="Window_Closing">

    <Window.Resources>
        <!-- 系统托盘图标 -->
        <tb:TaskbarIcon x:Key="TrayIcon"
                        ToolTipText="CodePanion"
                        TrayMouseDoubleClick="TrayIcon_TrayMouseDoubleClick">
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="显示主窗口" Click="ShowWindow_Click"/>
                    <Separator/>
                    <MenuItem Header="连接状态" x:Name="ConnectionStatusMenuItem" IsEnabled="False"/>
                    <Separator/>
                    <MenuItem Header="退出" Click="Exit_Click"/>
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>
    </Window.Resources>

    <Grid>
        <wv2:WebView2 x:Name="ChatWebView"/>

        <!-- Legacy named controls kept hidden for daemon/session plumbing. The WebView owns the visible shell. -->
        <Grid Visibility="Collapsed">
            <Ellipse x:Name="StatusIndicator" Width="12" Height="12" Fill="Gray"/>
            <TextBlock x:Name="StatusText" Text="未连接"/>
            <TextBlock x:Name="StatusBarText" Text="就绪"/>
            <Button x:Name="ReconnectButton" Content="重新连接" Click="Reconnect_Click"/>
            <TextBlock x:Name="VersionText" Text="v0.2.0"/>
            <TextBlock x:Name="SessionCountText" Text="0"/>
            <ListView x:Name="SessionListView" SelectionChanged="SessionListView_SelectionChanged"/>
        </Grid>
    </Grid>
</Window>
